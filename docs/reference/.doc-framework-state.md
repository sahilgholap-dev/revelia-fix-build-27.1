# Documentation framework — run state

> Machine/agent marker. Each framework run reads this first to scope its delta, then updates it. Do not hand-edit casually.

- **Last run**: 2026-07-25 (UPDATE run — scoped delta since 2026-07-17, 2 sonnet sub-agents dispatched for a mobile-code scan + a server-code scan since R7/R9 landed real mobile UI for the first time; commits `da42719`..`549cabd` on `feature/build-27`)
- **Last documented build**: Build 27, release-candidate state (branch `feature/build-27`, HEAD at run time `549cabd`. versionName bumped 1.2.0→2.0.0 same day; `mobile/app.json` apiUrl points at the production Railway backend after being toggled through staging/a custom domain). Build 26 = shipped baseline (vc26 live, still not merged to `main`).
- **Build-27 status captured**: R1 ✅ · R2 ✅ · R3 ✅ (owner step 10 only) · R4 ✅ · R5 ✅ (flag OFF) · R6 ✅ including Option C card (shipped 2026-07-25) · R7 🟢 CODE-COMPLETE, prod-dark, non-code launch gates only (rule-set R2 re-upload, one Sid wording confirm, 2 non-blocking config Qs) · R9 🔨 FEATURE-COMPLETE, prod-dark, `REPORT_WORKER_ENABLED` prod flip is the final gate · R8 not started.
- **Feature docs present**: astrology-swiss-ephemeris, face-extraction, palm-extraction, numerology, synthesis-engine, auth, push-notifications, subscriptions, continuity-readings, qa-timing-engine, cosmic-report. All 11 accurate as of this run.
- **Build docs present**: build-26.md (baseline, unchanged since bootstrap), build-27.md (delta, fully rewritten this run), changelog.md (3 entries).
- **Other docs present**: architecture/{overview,infrastructure,data-model}, server/overview, mobile/overview, packages/shared-types, history/early-development, setup/{getting-started,environment}, README. All except `architecture/overview.md` and `architecture/infrastructure.md` and `setup/getting-started.md` and `history/early-development.md` got edits this run (those four still accurate — R7/R9 didn't change the overall architecture story, just added surface area within it).
- **External work log**: `C:\Users\User\Downloads\mix\WORK_LOG.md` — last entry date **2026-07-25**; this run appended: R7 code-complete, R9 feature-complete, the 2.0.0 release-candidate version bump, R6 Option C, and the Ask-the-Stars/PP-only-gate/share-fix/bugfix bundle.
- **Session transcripts**: still deliberately NOT mined (curated equivalents: `tracking_files/claude_progress.md`, `tracking_files/session_handoff.md`, `tracking_files/build-26/`). Same policy holds.
- **Known TODO/verify gaps (carried, unchanged)**: Sentry/Mixpanel actually active in prod? · root `scripts/README.md` references a non-existent seed script.
- **Known TODO/verify gaps (updated/expanded this run)**: `server/.env.example` is missing `SENDGRID_*`, `SYNTHESIS_FABLE_ENABLED`, `QA_DEVICE_SALT`, `CRISIS_WORDING_FINALIZED`, `TIMING_CONFIG_DIR`, `REPORT_WORKER_ENABLED`, and all `R2_REPORTS_*` vars (all confirmed present in code via this run's server-delta scan). The old "API domain discrepancy" gap is now RESOLVED/explained: `api.revelia.me` was a custom-domain attempt tried and abandoned on 2026-07-25 in favor of the Railway URL — no longer a mystery, just release-day churn.
- **New gap noted this run**: neither R7 nor R9 is actually deployed/flag-flipped yet — the next run should check whether `REPORT_WORKER_ENABLED`/`SYNTHESIS_FABLE_ENABLED` flipped, whether the Timing Engine rule-set R2 re-upload happened, and whether build-27 actually shipped a release (versionCode, Play Store promotion, `feature/build-26`→`main` merge). If a release shipped, `builds/build-26.md`-style archival treatment should probably extend to a `build-27.md` baseline doc and rollover per `CLAUDE.md`'s "build rollover ritual."

## Update-run checklist (for the next run)

1. Diff repo state vs "Build-27 status captured" above (read `tracking_files/session_handoff.md` + latest commits).
2. **Check first whether build-27 has actually shipped** (see "New gap noted this run") — if so, this next run is a BUILD ROLLOVER, not just a delta: archive build-27 as the new baseline, start tracking build-28.
3. Otherwise, edit only docs whose subject changed; add new feature/build docs as needed (R8, the R9 "someone else" path turning on, build-28…).
4. APPEND a dated entry to `builds/changelog.md`; APPEND new entries to the external WORK_LOG (never duplicate — check its latest dates first).
5. Rewrite this file's fields.
